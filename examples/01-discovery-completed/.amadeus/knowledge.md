# ナレッジ

## 背景

以前の root `.amadeus/` は、実運用状態と例示成果物が混ざっていました。
この例示では、Amadeus 自身の改善テーマを使い、Discovery から段階的に Intent へ進める流れを示します。

## 前提

- 例示は `examples/<snapshot>/.amadeus/` に置きます。
- root `.amadeus/` は、リポジトリ自身の実運用状態としては置きません。
- Construction は実装証拠と結びつくため、今回の例示 snapshot には含めません。

## 未確認事項

- 実プロジェクトごとの独自テンプレート適用は、この例示では扱いません。
